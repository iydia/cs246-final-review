import BST;

import <iostream>;
import <string>;

using namespace std;

int main(int argc, char* argv[]) {
    BST b1;
    addToBST(b1, 1);
    addToBST(b1, 3);
    addToBST(b1, 5);
    addToBST(b1, 4);
    addToBST(b1, 7);
    cout << b1 << endl;
}
